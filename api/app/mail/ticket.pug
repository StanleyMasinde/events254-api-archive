extends ./layouts/main.pug
block content
    tr
        td.content-wrap
            table(width='100%' cellpadding='0' cellspacing='0')
                tr
                    td.content-block
                        h3 hi #{currentUser.name} 👋, your order has been confirmed
                tr
                    td.content-block
                        | You have booked #{ticketCount} ticket for 
                        b #{currentEvent.title} 
                        | The order ref number 
                        b #EV254-#{ticketId}
                tr
                    td.content-block
                        h3 About this event
                        h3 #{currentEvent.title}
                        p #{currentEvent.description}
                tr
                    td.content-block
                        | &mdash; Events254
//- TODO add QR code and PDF